# Porta em que a API Express escuta (padrão 3001)
PORT=3001

# Segredo usado para assinar tokens JWT — gere algo único em produção
JWT_SECRET=troque-este-valor-em-producao

# Chave esperada no header `x-admin-key` para rotas /admin/*
ADMIN_KEY=troque-este-valor-em-producao
